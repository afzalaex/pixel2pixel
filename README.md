## Version History

v0 – Basic canvas with ID-based URL seeding  
v1 – Color canvas + node up/down metrics  
v2 – Node unavailable metric on canvas  
v3 – Removed ID seeding; added mock minting with cipher seeding URLs  
v4 – Removed cipher URLs; added mock wallet seeding; unified single-page canvas + seeding; active node metrics  
v5 – Real wallet connection + on-chain minting (testnet)
